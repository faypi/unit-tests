export const findIndex = (array, value) => {
  return array.indexOf(value)
  // if (value == array[0])
  //   return 0

  // if (value == array[array.length - 1])
  //   return array.length - 1

  // if (value == array[(array.length - 1) / 2])
  //   return (array.length - 1) / 2

  // if (value !== array.value)
  //   return -1
}

